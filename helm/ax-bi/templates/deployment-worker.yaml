{{/*

 Licensed to the Apache Software Foundation (ASF) under one or more
 contributor license agreements.  See the NOTICE file distributed with
 this work for additional information regarding copyright ownership.
 The ASF licenses this file to You under the Apache License, Version 2.0
 (the "License"); you may not use this file except in compliance with
 the License.  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.

*/}}

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "axbi.fullname" . }}-worker
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "axbi.name" . }}-worker
    chart: {{ template "axbi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- if .Values.extraLabels }}
      {{- toYaml .Values.extraLabels | nindent 4 }}
    {{- end }}
    {{- if .Values.axbiWorker.deploymentLabels }}
      {{- toYaml .Values.axbiWorker.deploymentLabels | nindent 4 }}
    {{- end }}
  {{- if .Values.axbiWorker.deploymentAnnotations }}
  annotations: {{- toYaml .Values.axbiWorker.deploymentAnnotations | nindent 4 }}
  {{- end }}
spec:
  {{- if and (not .Values.axbiWorker.autoscaling.enabled) .Values.axbiWorker.replicas.enabled }}
  replicas: {{ .Values.axbiWorker.replicas.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
    {{- include "axbiWorker.selectorLabels" . | nindent 6 }}
  {{- if .Values.axbiWorker.strategy }}
  strategy: {{- toYaml .Values.axbiWorker.strategy | nindent 4 }}
  {{- end }}
  template:
    metadata:
      annotations:
        checksum/axbi_config.py: {{ include "axbi-config" . | sha256sum }}
        checksum/axbi_bootstrap.sh: {{ tpl .Values.bootstrapScript . | sha256sum }}
        checksum/connections: {{ .Values.axbiNode.connections | toYaml | sha256sum }}
        checksum/extraConfigs: {{ .Values.extraConfigs | toYaml | sha256sum }}
        checksum/extraSecrets: {{ .Values.extraSecrets | toYaml | sha256sum }}
        checksum/extraSecretEnv: {{ .Values.extraSecretEnv | toYaml | sha256sum }}
        checksum/configOverrides: {{ .Values.configOverrides | toYaml | sha256sum }}
        checksum/configOverridesFiles: {{ .Values.configOverridesFiles | toYaml | sha256sum }}
        {{- if .Values.axbiWorker.forceReload }}
        # Optionally force the thing to reload
        force-reload: {{ randAlphaNum 5 | quote }}
        {{- end }}
        {{- if .Values.axbiWorker.podAnnotations }}
          {{- toYaml .Values.axbiWorker.podAnnotations | nindent 8 }}
        {{- end }}
      labels:
        app: {{ template "axbi.name" . }}-worker
        release: {{ .Release.Name }}
        {{- if .Values.extraLabels }}
          {{- toYaml .Values.extraLabels | nindent 8 }}
        {{- end }}
        {{- if .Values.axbiWorker.podLabels }}
          {{- toYaml .Values.axbiWorker.podLabels | nindent 8 }}
        {{- end }}
    spec:
      {{- if or (.Values.serviceAccount.create) (.Values.serviceAccountName) }}
      serviceAccountName: {{ template "axbi.serviceAccountName" . }}
      {{- end }}
      securityContext:
        runAsUser: {{ .Values.runAsUser }}
        {{- if .Values.axbiWorker.podSecurityContext }}
          {{- toYaml .Values.axbiWorker.podSecurityContext | nindent 8 }}
        {{- end }}
      {{- if .Values.axbiWorker.initContainers }}
      initContainers: {{- tpl (toYaml .Values.axbiWorker.initContainers) . | nindent 6 }}
      {{- end }}
      {{- with .Values.hostAliases }}
      hostAliases: {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.axbiWorker.containerSecurityContext }}
          securityContext: {{- toYaml .Values.axbiWorker.containerSecurityContext | nindent 12 }}
          {{- end }}
          command: {{  tpl (toJson .Values.axbiWorker.command) . }}
          env:
            - name: "AXBI_PORT"
              value: {{ .Values.service.port | quote}}
            {{- range $key, $value := .Values.extraEnv }}
            - name: {{ $key | quote}}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.extraEnvRaw }}
            {{- toYaml .Values.extraEnvRaw | nindent 12 }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ tpl .Values.envFromSecret . | quote }}
            {{- range .Values.envFromSecrets }}
            - secretRef:
                name: {{ tpl . $ | quote }}
            {{- end }}
          volumeMounts:
            - name: axbi-config
              mountPath: {{ .Values.configMountPath | quote }}
              readOnly: true
            {{- if .Values.extraConfigs }}
            - name: axbi-extra-config
              mountPath: {{ .Values.extraConfigMountPath | quote }}
              readOnly: true
            {{- end }}
            {{- with .Values.extraVolumeMounts }}
              {{- tpl (toYaml .) $ | nindent 12 -}}
            {{- end }}
          {{- if .Values.axbiWorker.startupProbe }}
          startupProbe: {{- .Values.axbiWorker.startupProbe | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.axbiWorker.readinessProbe }}
          readinessProbe: {{- .Values.axbiWorker.readinessProbe | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.axbiWorker.livenessProbe }}
          livenessProbe: {{- .Values.axbiWorker.livenessProbe | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.axbiWorker.lifecycle }}
          lifecycle: {{- .Values.axbiWorker.lifecycle | toYaml | nindent 12 }}
          {{- end }}
          resources:
            {{- if .Values.axbiWorker.resources }}
              {{- toYaml .Values.axbiWorker.resources | nindent 12 }}
            {{- else }}
              {{- toYaml .Values.resources | nindent 12 }}
            {{- end }}
        {{- if .Values.axbiWorker.extraContainers }}
          {{- tpl (toYaml .Values.axbiWorker.extraContainers) . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.affinity .Values.axbiWorker.affinity }}
      affinity:
        {{- with .Values.affinity }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.axbiWorker.affinity }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- if .Values.axbiWorker.priorityClassName }}
      priorityClassName: {{ .Values.axbiWorker.priorityClassName }}
      {{- end }}
      {{- if or .Values.topologySpreadConstraints .Values.axbiWorker.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- with .Values.topologySpreadConstraints }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.axbiWorker.topologySpreadConstraints }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.axbiWorker.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.axbiWorker.terminationGracePeriodSeconds }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets: {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      volumes:
        - name: axbi-config
          secret:
            secretName: {{ tpl .Values.configFromSecret . }}
        {{- if .Values.extraConfigs }}
        - name: axbi-extra-config
          configMap:
            name: {{ template "axbi.fullname" . }}-extra-config
        {{- end }}
        {{- with .Values.extraVolumes }}
          {{- tpl (toYaml .) $ | nindent 8 -}}
        {{- end }}
