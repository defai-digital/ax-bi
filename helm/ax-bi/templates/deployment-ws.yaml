{{/*

 Licensed to the Apache Software Foundation (ASF) under one or more
 contributor license agreements.  See the NOTICE file distributed with
 this work for additional information regarding copyright ownership.
 The ASF licenses this file to You under the Apache License, Version 2.0
 (the "License"); you may not use this file except in compliance with
 the License.  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.

*/}}

{{- if .Values.axbiWebsockets.enabled }}
{{- if eq .Values.axbiWebsockets.config.jwtSecret "CHANGE-ME" }}
  {{- fail "axbiWebsockets.config.jwtSecret must be changed from the default 'CHANGE-ME' value to a strong secret" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ template "axbi.fullname" . }}-ws"
  namespace: {{ .Release.Namespace }}
  labels:
    app: "{{ template "axbi.name" . }}-ws"
    chart: {{ template "axbi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- if .Values.extraLabels }}
      {{- toYaml .Values.extraLabels | nindent 4 }}
    {{- end }}
  {{- if .Values.axbiWebsockets.deploymentAnnotations }}
  annotations: {{- toYaml .Values.axbiWebsockets.deploymentAnnotations | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.axbiWebsockets.replicaCount }}
  selector:
    matchLabels:
    {{- include "axbiWebsockets.selectorLabels" . | nindent 6 }}
  {{- if .Values.axbiWebsockets.strategy }}
  strategy: {{- toYaml .Values.axbiWebsockets.strategy | nindent 4 }}
  {{- end }}
  template:
    metadata:
      annotations:
        checksum/wsconfig: {{ tpl (toJson .Values.axbiWebsockets.config) . | sha256sum }}
        checksum/secrets: {{ tpl (toJson .Values.extraSecretEnv) . | sha256sum }}
        {{- if .Values.axbiWebsockets.podAnnotations }}
          {{- toYaml .Values.axbiWebsockets.podAnnotations | nindent 8 }}
        {{- end }}
      labels:
        app: "{{ template "axbi.name" . }}-ws"
        release: {{ .Release.Name }}
        {{- if .Values.extraLabels }}
          {{- toYaml .Values.extraLabels | nindent 8 }}
        {{- end }}
        {{- if .Values.axbiWebsockets.podLabels }}
          {{- toYaml .Values.axbiWebsockets.podLabels | nindent 8 }}
        {{- end }}
    spec:
      {{- if or (.Values.serviceAccount.create) (.Values.serviceAccountName) }}
      serviceAccountName: {{ template "axbi.serviceAccountName" . }}
      {{- end }}
      securityContext:
        runAsUser: {{ .Values.runAsUser }}
        {{- if .Values.axbiWebsockets.podSecurityContext }}
          {{- toYaml .Values.axbiWebsockets.podSecurityContext | nindent 8 }}
        {{- end }}
      {{- with .Values.hostAliases }}
      hostAliases: {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-ws"
          image: "{{ .Values.axbiWebsockets.image.repository }}:{{ .Values.axbiWebsockets.image.tag }}"
          imagePullPolicy: {{ .Values.axbiWebsockets.image.pullPolicy }}
          {{- if .Values.axbiWebsockets.containerSecurityContext }}
          securityContext: {{- toYaml .Values.axbiWebsockets.containerSecurityContext | nindent 12 }}
          {{- end }}
          command: {{  tpl (toJson .Values.axbiWebsockets.command) . }}
          # Passing all the envs is a bit blunt... we only need a few (see https://github.com/defai-digital/ax-bi/blob/main/ax-bi-websocket/src/config.ts)...
          env:
            {{- range $key, $value := .Values.extraEnv }}
            - name: {{ $key | quote}}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.extraEnvRaw }}
              {{- toYaml .Values.extraEnvRaw | nindent 12 }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ tpl .Values.envFromSecret . | quote }}
            {{- range .Values.envFromSecrets }}
            - secretRef:
                name: {{ tpl . $ | quote }}
            {{- end }}
          ports:
            - name: ws
              containerPort: {{ .Values.axbiWebsockets.config.port }}
              protocol: TCP
          volumeMounts:
            - name: axbi-ws-config
              mountPath: /home/ax-bi-websocket/config.json
              subPath: config.json
              readOnly: true
          resources:
            {{- if .Values.axbiWebsockets.resources }}
              {{- toYaml .Values.axbiWebsockets.resources | nindent 12 }}
            {{- else }}
              {{- toYaml .Values.resources | nindent 12 }}
            {{- end }}
          {{- if .Values.axbiWebsockets.startupProbe }}
          startupProbe: {{- .Values.axbiWebsockets.startupProbe | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.axbiWebsockets.readinessProbe }}
          readinessProbe: {{- .Values.axbiWebsockets.readinessProbe | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.axbiWebsockets.livenessProbe }}
          livenessProbe: {{- .Values.axbiWebsockets.livenessProbe | toYaml | nindent 12 }}
          {{- end }}
        {{- if .Values.axbiWebsockets.extraContainers }}
            {{- tpl (toYaml .Values.axbiWebsockets.extraContainers) . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.affinity .Values.axbiWebsockets.affinity }}
      affinity:
        {{- with .Values.affinity }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.axbiWebsockets.affinity }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- if .Values.axbiWebsockets.priorityClassName }}
      priorityClassName: {{ .Values.axbiWebsockets.priorityClassName }}
      {{- end }}
      {{- if or .Values.topologySpreadConstraints .Values.axbiWebsockets.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- with .Values.topologySpreadConstraints }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.axbiWebsockets.topologySpreadConstraints }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:  {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets: {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      volumes:
        - name: axbi-ws-config
          secret:
            secretName: "{{ template "axbi.fullname" . }}-ws-config"
{{- end }}
