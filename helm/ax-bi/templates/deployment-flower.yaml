{{/*

 Licensed to the Apache Software Foundation (ASF) under one or more
 contributor license agreements.  See the NOTICE file distributed with
 this work for additional information regarding copyright ownership.
 The ASF licenses this file to You under the Apache License, Version 2.0
 (the "License"); you may not use this file except in compliance with
 the License.  You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

 Unless required by applicable law or agreed to in writing, software
 distributed under the License is distributed on an "AS IS" BASIS,
 WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 See the License for the specific language governing permissions and
 limitations under the License.

*/}}

{{- if .Values.axbiCeleryFlower.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "axbi.fullname" . }}-flower
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "axbi.name" . }}-flower
    chart: {{ template "axbi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    {{- if .Values.extraLabels }}
      {{- toYaml .Values.extraLabels | nindent 4 }}
    {{- end }}
  {{- if .Values.axbiCeleryFlower.deploymentAnnotations }}
  annotations: {{- toYaml .Values.axbiCeleryFlower.deploymentAnnotations | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.axbiCeleryFlower.replicaCount }}
  selector:
    matchLabels:
    {{- include "axbiCeleryFlower.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include "axbi-config" . | sha256sum }}
        checksum/secrets: {{ tpl (toJson .Values.extraSecretEnv) . | sha256sum }}
        {{- if .Values.axbiCeleryFlower.podAnnotations }}
          {{- toYaml .Values.axbiCeleryFlower.podAnnotations | nindent 8 }}
        {{- end }}
      labels:
        app: "{{ template "axbi.name" . }}-flower"
        release: {{ .Release.Name }}
        {{- if .Values.extraLabels }}
          {{- toYaml .Values.extraLabels | nindent 8 }}
        {{- end }}
        {{- if .Values.axbiCeleryFlower.podLabels }}
          {{- toYaml .Values.axbiCeleryFlower.podLabels | nindent 8 }}
        {{- end }}
    spec:
      {{- if or (.Values.serviceAccount.create) (.Values.serviceAccountName) }}
      serviceAccountName: {{ template "axbi.serviceAccountName" . }}
      {{- end }}
      securityContext:
        runAsUser: {{ .Values.runAsUser }}
        {{- if .Values.axbiCeleryFlower.podSecurityContext }}
          {{- toYaml .Values.axbiCeleryFlower.podSecurityContext | nindent 8 }}
        {{- end }}
      {{- if .Values.axbiCeleryFlower.initContainers }}
      initContainers: {{-  tpl (toYaml .Values.axbiCeleryFlower.initContainers) . | nindent 6 }}
      {{- end }}
      {{- with .Values.hostAliases }}
      hostAliases: {{- toYaml . | nindent 6 }}
      {{- end }}
      containers:
        - name: "{{ .Chart.Name }}-flower"
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- if .Values.axbiCeleryFlower.containerSecurityContext }}
          securityContext: {{- toYaml .Values.axbiCeleryFlower.containerSecurityContext | nindent 12 }}
          {{- end }}
          command: {{  tpl (toJson .Values.axbiCeleryFlower.command) . }}
          env:
            {{- range $key, $value := .Values.extraEnv }}
            - name: {{ $key | quote}}
              value: {{ $value | quote }}
            {{- end }}
            {{- if .Values.extraEnvRaw }}
              {{- toYaml .Values.extraEnvRaw | nindent 12 }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ tpl .Values.envFromSecret . | quote }}
            {{- range .Values.envFromSecrets }}
            - secretRef:
                name: {{ tpl . $ | quote }}
            {{- end }}
          ports:
            - name: flower
              containerPort: 5555
              protocol: TCP
          volumeMounts:
            - name: axbi-config
              mountPath: {{ .Values.configMountPath | quote }}
              readOnly: true
            {{- with .Values.extraVolumeMounts }}
              {{- tpl (toYaml .) $ | nindent 12 -}}
            {{- end }}
          {{- if .Values.axbiCeleryFlower.startupProbe }}
          startupProbe: {{- .Values.axbiCeleryFlower.startupProbe | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.axbiCeleryFlower.readinessProbe }}
          readinessProbe: {{- .Values.axbiCeleryFlower.readinessProbe | toYaml | nindent 12 }}
          {{- end }}
          {{- if .Values.axbiCeleryFlower.livenessProbe }}
          livenessProbe: {{- .Values.axbiCeleryFlower.livenessProbe | toYaml | nindent 12 }}
          {{- end }}
          resources:
            {{- if .Values.axbiCeleryFlower.resources }}
              {{- toYaml .Values.axbiCeleryFlower.resources | nindent 12 }}
            {{- else }}
              {{- toYaml .Values.resources | nindent 12 }}
            {{- end }}
        {{- if .Values.axbiCeleryFlower.extraContainers }}
            {{- tpl (toYaml .Values.axbiCeleryFlower.extraContainers) . | nindent 8 }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.affinity .Values.axbiCeleryFlower.affinity }}
      affinity:
        {{- with .Values.affinity }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.axbiCeleryFlower.affinity }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- if .Values.axbiCeleryFlower.priorityClassName }}
      priorityClassName: {{ .Values.axbiCeleryFlower.priorityClassName }}
      {{- end }}
      {{- if or .Values.topologySpreadConstraints .Values.axbiCeleryFlower.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- with .Values.topologySpreadConstraints }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.axbiCeleryFlower.topologySpreadConstraints }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations: {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets: {{- toYaml .Values.imagePullSecrets | nindent 8 }}
      {{- end }}
      volumes:
        - name: axbi-config
          secret:
            secretName: {{ tpl .Values.configFromSecret . }}
        {{- with .Values.extraVolumes }}
          {{- tpl (toYaml .) $ | nindent 8 -}}
        {{- end }}
{{- end -}}
