# Copy this file to docker/.env-axbi and fill in the required values before
# running docker-compose-axbi.yml.
#
# Generate secrets:
#   openssl rand -base64 42

COMPOSE_PROJECT_NAME=axbi

# Required secrets
SUPERSET_SECRET_KEY=
DATABASE_PASSWORD=
ADMIN_PASSWORD=

# Image settings. Override these to pin a release tag or use private images.
AXBI_IMAGE=ghcr.io/defai-digital/ax-bi:latest
AX_SERVICES_IMAGE=ghcr.io/defai-digital/ax-bi-services:latest

# Service ports
SUPERSET_PORT=8088
MCP_PORT=5008
AX_SERVICES_PORT=5010

# Metadata database
DATABASE_DB=superset
DATABASE_USER=superset
DATABASE_HOST=db
DATABASE_PORT=5432
DATABASE_DIALECT=postgresql
POSTGRES_DB=superset
POSTGRES_USER=superset

# Runtime behavior
SUPERSET_LOAD_EXAMPLES=no
SUPERSET_LOG_LEVEL=info
ENABLE_PROXY_FIX=true

# MCP authentication.
# For a local single-user trial, set MCP_DEV_USERNAME=admin.
# For production, leave MCP_DEV_USERNAME empty and configure JWT settings.
MCP_DEV_USERNAME=
MCP_AUTH_ENABLED=false
MCP_JWT_ISSUER=
MCP_JWT_AUDIENCE=
MCP_JWKS_URI=
MCP_REQUIRED_SCOPES=
