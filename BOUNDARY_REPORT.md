# AX-BI Boundary Report

Scope: active project only (`/Users/devop/Documents/ax-bi/ax-bi`). Reference/vendor folders were not inspected or modified.

## Findings

| Area | Evidence | Boundary issue | Severity | Fix risk | Proposed action |
| --- | --- | --- | --- | --- | --- |
| MCP list response serialization | `superset/mcp_service/query/schemas.py` and `superset/mcp_service/saved_query/schemas.py` both implemented identical serializer logic. | Privacy filtering and selected-column projection were duplicated in each resource schema. | Medium | Low | ✅ Done (improvements #1–#4). Shared projection helpers in `utils/response_utils.py`; all resource schemas updated. |
| MCP tool modules and Superset globals | Runtime `event_logger` access is centralized in `superset/mcp_service/utils/logging_utils.py`; repeated `current_app.config` reads are centralized in `superset/mcp_service/utils/config_utils.py`. `rg` still shows direct MCP access to `db`, `security_manager`, `current_app`, `g`, and Flask app/config objects in modules such as `superset/mcp_service/auth.py`, `superset/mcp_service/middleware.py`, `superset/mcp_service/caching.py`, and `superset/mcp_service/storage.py`. | Tool transport, auth, configuration, and persistence boundaries remain blurred, making future service/runtime swaps harder. | High | Medium | Do not refactor broadly yet. Introduce small shared adapters only where a local duplication or confirmed test gap exists. |
| Frontend list pages | `ChartList`, `DashboardList`, `DatasetList`, `SavedQueryList`, `TaskList`, `ThemeList`, and `UsersList` repeat `useListViewResource`, `PAGE_SIZE = 25`, bulk action wiring, permission checks, filters, and sorting. | Page components mix list-state orchestration, permission policy, API shape, and rendering. This makes UI behavior changes broad and easy to drift. | Medium | Medium | Defer broad extraction. Start later with a tiny shared constant/helper only after one repeated behavior is covered by tests. |
| Report command clock handling | `superset/commands/report/execute.py` mixes report-state persistence and elapsed-time checks directly. It now uses the shared `naive_utcnow()` helper for DB timestamp consistency. | Report execution still owns several timing branches, so future clock-policy changes need care. | Low | Low | Leave the command flow as-is; the shared helper is enough for the current confirmed timestamp boundary issue. |
| Pandas postprocessing compatibility | `pivot.py`, `resample.py`, `rolling.py`, `boxplot.py`, and `utils.py` contain version-compatibility logic close to transformation code. | Compatibility shims are mixed into data transforms. | Low | Medium | Leave as-is for now because the functions are small and covered by focused tests. Extract only if more pandas-version branches appear. |
| Naive UTC database timestamp policy | `superset/commands/report/execute.py`, `superset/commands/logs/prune.py`, `superset/commands/report/log_prune.py`, and `superset/daos/log.py` each computed `datetime.now(timezone.utc).replace(tzinfo=None)` or a local `_utcnow()` for DB timestamp comparisons. | Timestamp storage policy was repeated across command/DAO boundaries. Future timezone fixes could drift. | Medium | Low | ✅ Done (improvements #5–#6). Centralized `naive_utcnow()` in `superset/utils/dates.py` and aligned all callers. |
| Prune command cutoff drift | `superset/commands/tasks/prune.py` and `superset/commands/sql_lab/query.py` computed retention cutoffs with `datetime.now()`, while task `ended_at` is documented as UTC and SQL Lab `changed_on` defaults to `datetime.utcnow`. | Retention policy crossed command/model boundaries inconsistently, so local server timezone could change which rows were pruned. | Medium | Low | ✅ Done (improvement #6). Aligned task and SQL Lab prune cutoffs with the shared `naive_utcnow()` helper. |
| Prune command batch deletion | `superset/commands/logs/prune.py`, `superset/commands/tasks/prune.py`, and `superset/commands/sql_lab/query.py` repeated the same SQLite-safe ID batching, commit-after-batch, progress logging, and elapsed-time logging. | Resource-specific retention selection was mixed with generic batch deletion mechanics. Future fixes to prune safety or progress reporting had to be repeated across commands. | Medium | Low | ✅ Done (improvement #11). Extracted the shared batch-delete/progress helper while keeping each prune command's selection query local. |
| MCP select-column request parsing | Many MCP list request schemas repeated a validator that called `parse_json_or_list(v, "select_columns")`. `ModelListCore` also parsed `select_columns` with the generic helper and a repeated literal. Get-info requests repeated the same default-column fallback. | Parsing policy for the common `select_columns` request parameter was duplicated across resource schemas. | Low | Low | ✅ Done (improvement #12). Centralized `parse_select_columns()` and defaulting helpers in schema utilities. |
| MCP list pagination boundary | Fourteen MCP list tools converted request pages with `page=max(request.page - 1, 0)` before calling `ModelListCore`. | Tool modules repeated the conversion between public 1-based request semantics and internal 0-based core pagination. | Low | Low | ✅ Done (improvement #13). Centralized page conversion through a tiny helper at list-tool call sites. |
| MCP error timestamp factories | Resource error schemas (`TagError`, `UserError`, `TaskError`, `ReportError`, `DatabaseError`, `RlsFilterError`, `RoleError`, `DatasetError`, `DashboardError`, `AnnotationLayerError`, `QueryError`, `SavedQueryError`) repeated timestamped `create()` factories and identical field declarations. | Error timestamp policy and field shape were duplicated across 12 resource schemas. Future timestamp behavior changes would need repeated edits. | Low | Low | ✅ Done (improvement #21). Introduced `MCPResourceError` base class in `common/error_schemas.py` with shared fields, `create()`, and opt-in LLM error-text sanitization. All 12 domain error classes now inherit from it. |
| MCP default request materialization | Optional MCP list tools cached a module-level default request and repeated `request = request or _DEFAULT_...model_copy(deep=True)`. | The policy that omitted requests should receive an isolated copy of the default Pydantic model was repeated across tool modules. | Low | Low | ✅ Done (improvement #15). Centralized through `request_or_default()` helper. |
| MCP search/filter mutual exclusion | List request schemas implement the same `if self.search and self.filters: raise ValueError(...)` rule via the shared `ensure_search_and_filters_not_combined()` helper. | Request validation policy helper is centralized; each schema still wraps the call with a resource-specific error message. | Low | Low | ✅ Done (improvement #16). Helper centralized; per-schema wrappers retained for resource-specific error messages. |
| MCP error text sanitization | `MCPBaseError` now sanitizes `message` at the base level. `MCPResourceError` sanitizes `error` for all 12 domain resource errors. `DashboardOperationResult` retains its own nullable-error validator. | The LLM-boundary policy for error text was duplicated across some resource schemas and missing from others. | Low | Low | ✅ Done (improvements #17, #21, #22). `MCPBaseError.message` and `MCPResourceError.error` both sanitize by default. `DashboardOperationResult` keeps a local validator for its nullable error shape. |
| MCP structured filters parsing | List request schemas call `parse_filters(value, <ResourceFilter>)` via the shared helper. Each schema keeps its resource-specific filter model. | The common request-parameter parser is centralized; each schema retains a thin wrapper for its resource-specific filter type. | Low | Low | ✅ Done (improvement #18). `parse_filters()` helper centralized; per-schema wrappers retained for type-specific filter models. |
| MCP parser type boundaries | `parse_json_or_model`, `parse_json_or_model_list`, and `parse_filters` are generic over the supplied model class. | The schema utility boundary now preserves resource-specific model types via `TypeVar`. | Low | Low | ✅ Done (improvement #19). Parser helpers are generic; redundant casts removed. |
| MCP schema resource payloads | `superset/mcp_service/common/schema_discovery.py` owns both the columns/default/sort/search metadata and the resource payload builder. `system/resources/schema_discovery.py` is a thin registration layer. | Schema discovery metadata is centralized in common helpers. | Low | Low | ✅ Done (improvement #20). Resource payload builder moved into common schema discovery. |

## Improvements

1. Implemented the low-risk MCP response serialization helper. This reduces duplicated policy logic without changing API behavior or moving tool architecture.
2. Applied the same helper to MCP schemas that already use `filter_user_directory_fields` with no resource-specific serializer behavior: `chart`, `dashboard`, `database`, and `report`.
3. Split select-column projection into its own helper so MCP schemas that do not apply privacy filtering (`tag`, `task`, `role`, `user`, and `rls`) can share projection behavior without gaining new privacy side effects.
4. Dataset schema keeps custom behavior for nested `column_fields` and `schema_name` alias normalization, then reuses the select-column projection helper after those dataset-specific steps.
5. Centralized naive UTC DB timestamp creation in `superset/utils/dates.py` and replaced local copies in report execution, log pruning, report log pruning, and log DAO code.
6. Aligned task and SQL Lab query prune cutoffs with the shared naive UTC helper. This avoids local-time retention drift without changing pruning batch behavior.
7. Centralized the default CRUD list page size in `src/views/CRUD/constants.ts` and pointed the main list pages at it, while leaving intentionally different page sizes untouched.
8. Replaced the remaining same-semantics `PAGE_SIZE = 25` constants in CRUD list pages with `DEFAULT_LIST_PAGE_SIZE`. Test literals and component-specific pagination sizes remain unchanged.
9. Moved repeated MCP list response dumps with `context={"select_columns": ...}` into a small response utility. Each tool keeps its DAO, filters, and event logging intact.
10. Reused the same MCP response dump utility in single-resource info tools (`get_chart_info`, `get_dashboard_info`, `get_dataset_info`) where they apply the same select-column serialization context.
11. Extracted the duplicated prune batch-delete loop into a shared command utility while leaving each prune command's selection query local.
12. Centralized MCP `select_columns` request parsing through small schema utilities, including list schemas, get-info default-column fallback, and the shared list-core column-loading path, without introducing shared request base classes.
13. Centralized MCP list page-number conversion through a tiny helper so list tools do not repeat the public 1-based to core 0-based boundary rule.
14. Centralized MCP resource error timestamp creation so schema factories do not repeat UTC timestamp policy.
15. Centralized MCP default request materialization so optional list tools do not repeat deep-copy default handling.
16. Centralized MCP list request search/filter mutual-exclusion validation while preserving existing error messages.
17. Centralized MCP error text sanitization through a common helper while keeping schema validators local to each response model.
18. Centralized MCP structured `filters` parsing while keeping each resource schema's filter model local.
19. Preserved resource-specific model types across MCP schema parser helpers and removed redundant casts.
20. Centralized MCP schema resource payload construction in common schema discovery helpers.
21. Consolidated 12 domain MCP resource error classes behind a shared `MCPResourceError` base in `common/error_schemas.py`. The base provides shared fields (`error`, `error_type`, `timestamp`), `model_config`, `create()` factory, and opt-in LLM error-text sanitization. All 12 resource error schemas now inherit from it, eliminating ~120 lines of duplicated field declarations, factory methods, and config settings. This also fixed inconsistent sanitization coverage — six error classes that previously skipped LLM error-text sanitization now apply it by default.
22. Moved LLM error-text sanitization into `MCPBaseError.message` so all general MCP error classes (`ChartError`, `ChartGenerationError`) inherit it automatically. Removed the redundant `ChartError` validator. This extends sanitization coverage to `ChartGenerationError`, which previously did not sanitize its `message` field.
23. Fixed distributed-lock / key-value expiry UTC consistency (BUG_REPORT ISSUE-1). Aligned all 8 call sites across 4 files (`acquire.py`, `key_value/models.py`, `key_value/commands/prune.py`, `daos/key_value.py`) to use the shared `naive_utcnow()` helper instead of mixing aware-UTC writes with naive-local comparisons. Updated the prune TOCTOU test to mock `naive_utcnow` instead of `datetime`. Also merged the duplicate `@apache-superset/core/theme` import in `ChartList/index.tsx`.
24. Simplified MCP `get_info` tool error handling by using `MCPResourceError.create()` instead of direct construction with `datetime.now(timezone.utc)`. Removed `datetime` imports from 6 tool files (`get_annotation_layer_info.py`, `get_layer_annotation_info.py`, `get_dashboard_info.py`, `get_dashboard_layout.py`, `get_database_info.py`, `get_dataset_info.py`) and centralized timestamp handling through the shared error base class.
25. Extracted MCP list tool post-query pipeline into `finalize_list_response()` helper in `response_utils.py`. Removed duplicated count/logging, column extraction, and serialization context wiring from 5 list tools (`list_charts.py`, `list_dashboards.py`, `list_databases.py`, `list_datasets.py`, `list_annotation_layers.py`).
26. Simplified 5 MCP tool error handlers (`get_task_info.py`, `get_tag_info.py`, `get_rls_filter_info.py`, `get_report_info.py`, `list_reports.py`) to use `MCPResourceError.create()` instead of direct construction with `datetime.now(timezone.utc)`. Removed `datetime` imports from all 5 files and centralized timestamp handling through the shared error base class.
27. Made `PrivacyError` in `privacy.py` inherit from `MCPResourceError` instead of standalone `BaseModel`. Removed duplicated field declarations (`error`, `error_type`, `timestamp`, `model_config`) and `datetime` import. This closes the last LLM-boundary sanitization gap — `PrivacyError.error` is now sanitized via the inherited `MCPResourceError` validator, consistent with all 12 resource error classes and `MCPBaseError`. Timestamp construction now uses the shared `mcp_error_timestamp()` factory.
28. Replaced direct error construction in `ModelGetInfoCore.run_tool()` (`mcp_core.py`) with `self.error_schema.create()`. Removed the inline `datetime.now(timezone.utc)` timestamp, centralizing error timestamp policy through the shared `MCPResourceError` factory. This completes the error-handling consolidation started by improvements #24 and #26 — the MCP core framework layer no longer constructs errors directly.
29. Centralized response timestamp creation in `mcp_core.py` through the shared `mcp_error_timestamp()` factory. Replaced `datetime.now(timezone.utc)` in `ModelListCore._build_response_dict()` (line 391) and `datetime.now(timezone.utc).isoformat()` in `InstanceInfoCore.run_tool()` (line 746). The `_calculate_time_based_metrics()` usage (line 646) was intentionally left unchanged — it is legitimate time-window computation (`now - timedelta(days=days)`), not a timestamp policy call.
30. Extended `finalize_list_response()` helper with optional `by_alias` parameter and applied it to 6 remaining list tools (`list_reports`, `list_queries`, `list_saved_queries`, `list_roles`, `list_tags`, `list_tasks`, `list_users`). This completes the post-query pipeline consolidation started by improvement #25 — all standard MCP list tools now use the shared helper. Tools with custom column logic (`list_rls_filters`, `list_layer_annotations`) retain their specialized patterns.
31. Introduced a small MCP event logging utility boundary in `utils/logging_utils.py` and routed the shared list-response serialization pipeline through it. This begins reducing direct `superset.extensions.event_logger` coupling from common MCP utilities without changing individual tool behavior or broad logging architecture.
32. Routed MCP list-tool query logging through `mcp_event_log_context()` across 13 `list_*` tools. This removes direct `superset.extensions.event_logger` imports from the repeated list-query/serialization path while preserving the exact event action names and leaving each tool's DAO, filter, security, and pagination behavior unchanged.
33. Routed MCP read-only get-info/layout tool logging through `mcp_event_log_context()` across 15 `get_*_info`/layout tools. This extends the event-logging adapter boundary to lookup and serialization contexts while preserving the exact event action names and keeping each tool's model lookup, serializer, and error handling unchanged.
34. Routed small MCP system/discovery tool logging through `mcp_event_log_context()` for `health_check`, `generate_bug_report`, `get_schema`, `get_instance_info`, and `find_users`. This removes direct `event_logger` coupling from read-only system utility paths while leaving DB/session, RBAC, privacy, and environment collection behavior unchanged.
35. Routed read/explore MCP tool logging through `mcp_event_log_context()` for `generate_explore_link` and `get_chart_sql`. This removes direct `event_logger` coupling from chart URL/SQL read paths while leaving dataset access checks, chart validation, URL generation, SQL construction, and cache reads unchanged.
36. Routed SQL Lab MCP tool logging through `mcp_event_log_context()` for `execute_sql`, `save_sql_query`, and `open_sql_lab_with_context`. This removes direct `event_logger` coupling from SQL Lab validation/execution/response scopes while leaving SQL parsing, database access checks, query execution, saved-query commits, URL generation, and rollback behavior unchanged.
37. Routed dataset creation MCP logging through `mcp_event_log_context()` for `create_dataset` and `create_virtual_dataset`. This removes direct `event_logger` coupling from dataset create/update logging scopes while leaving dataset command execution, metadata update rollback, validation mapping, and serialization unchanged.
38. Routed dataset semantic-query MCP logging through `mcp_event_log_context()` for `query_dataset`. This removes direct `event_logger` coupling from dataset lookup and query execution logging scopes while leaving metadata privacy checks, semantic query construction, ChartDataCommand execution, cache metadata, and response formatting unchanged.
39. Routed chart preview-update MCP logging through `mcp_event_log_context()` for `update_chart_preview`. This removes direct `event_logger` coupling from cached preview lookup/form-data/metadata/preview scopes while leaving dataset access checks, form-data merging, validation, preview generation, and OAuth error handling unchanged.
40. Routed dashboard mutation MCP logging through `mcp_event_log_context()` for `generate_dashboard` and `add_chart_to_existing_dashboard`. This removes direct `event_logger` coupling from dashboard chart-validation/layout/db-write scopes while leaving dashboard authorization, layout construction, DB writes, refetch/fallback behavior, and serialization unchanged.
41. Routed chart generation MCP logging through `mcp_event_log_context()` for `generate_chart`. This removes direct `event_logger` coupling from validation, dataset lookup, DB write, compile-check, form-data cache, and preview scopes while leaving chart compilation, dataset access checks, save/rollback behavior, preview URL generation, and response serialization unchanged.
42. Routed chart update MCP logging through `mcp_event_log_context()` for `update_chart`. This removes direct `event_logger` coupling from chart lookup, validation, DB write, preview-link, and preview scopes while leaving chart lookup, dataset access validation, update command execution, preview URL creation, OAuth handling, and response serialization unchanged.
43. Routed chart preview retrieval MCP logging through `mcp_event_log_context()` for `get_chart_preview`. This removes direct `event_logger` coupling from chart lookup, unsaved form-data cache lookup, unsaved-state override, preview generation, and metadata scopes while leaving DB access, cached form-data handling, preview strategy selection, OAuth handling, and response serialization unchanged.
44. Routed chart data retrieval MCP logging through `mcp_event_log_context()` for `get_chart_data`. This removes direct `event_logger` coupling from unsaved form-data cache lookup, chart lookup, unsaved-state override, query execution, and export-format conversion scopes while leaving query-context construction, cached form-data handling, ChartDataCommand execution, cache metadata, CSV/Excel export, OAuth handling, and response serialization unchanged.
45. Added `mcp_event_log()` to the MCP logging utility and routed middleware event writes through it. This removes the last runtime direct `event_logger` dependency from MCP middleware while leaving tool-call, message, error, rate-limit, truncation, and size-exceeded event payloads and exception handling unchanged.
46. Added `get_superset_row_limit()` in `utils/config_utils.py` and routed chart data row-limit fallback reads through it. This removes direct `current_app.config["ROW_LIMIT"]` access from `get_chart_data` while leaving the existing fallback order and missing-config `KeyError` behavior unchanged.
47. Added `get_superset_app_name()` in `utils/config_utils.py` and routed health check, bug report environment collection, and quickstart prompt branding through it. This removes repeated direct `current_app.config.get("APP_NAME", "Superset")` reads while preserving existing default and missing-Flask-context behavior.
48. Added `get_mcp_bug_report_contact()` in `utils/config_utils.py` and routed bug report support-contact lookup through it. This removes direct Flask config access from `generate_bug_report` while preserving the local blank-string/default-contact policy and missing-Flask-context fallback.
49. Added `get_mcp_rbac_enabled()` in `utils/config_utils.py` and routed auth/tool-visibility/schema RBAC config checks through it. This removes repeated direct `current_app.config.get("MCP_RBAC_ENABLED", True)` reads while preserving the default-enabled policy and existing Python truthiness semantics.
50. Added auth-focused config helpers (`get_mcp_dev_username()`, `get_mcp_auth_enabled()`, `is_mcp_jwt_configured()`) and routed `get_user_from_request()` through them. This removes direct auth config reads from the user-resolution path while preserving the JWT/API-key/dev-user/g.user priority order and server-side diagnostic wording.
51. Added `get_mcp_jwt_algorithm()` in `utils/config_utils.py` and routed JWT verifier weak-config detection through it. This removes direct Flask config access from `jwt_verifier.py` while preserving the no-app-context constructor fallback to the explicit algorithm argument.
52. Added `get_superset_admin_role_name()` in `utils/config_utils.py` and routed MCP field-permission admin-bypass checks through it. This removes direct `current_app.config["AUTH_ROLE_ADMIN"]` access from `permissions_utils.py` while preserving configured-role behavior and missing-key `KeyError` handling inside the existing broad permission-check guard.
53. Added screenshot timing/error config helpers in `utils/config_utils.py` and routed pooled chart/dashboard screenshot waits through them. This removes duplicated direct screenshot config reads from `pooled_screenshot.py` while preserving existing indexed-key `KeyError` behavior for required timing settings and `.get()` behavior for unexpected-error replacement.
54. Added `get_webdriver_type()` in `utils/config_utils.py` and routed WebDriver pool driver creation through it. This removes direct `current_app.config.get("WEBDRIVER_TYPE", "firefox")` access from `webdriver_pool.py` while preserving the existing default Firefox behavior.
55. Added `get_webdriver_pool_config()` in `utils/config_utils.py` and routed global WebDriver pool initialization through it. This removes the remaining direct Flask `current_app.config` dependency from `webdriver_pool.py` while preserving the existing default empty-pool-config behavior and raw configured value semantics.
56. Added URL-focused config helpers (`get_webdriver_baseurl_user_friendly()`, `get_mcp_service_url_config()`) and routed MCP URL utility base/service URL construction through them. This removes direct Flask `current_app.config` access from `url_utils.py` while preserving the existing localhost fallbacks, explicit MCP service URL override, remote `/mcp` derivation, and broad config-access exception handling.
57. Added runtime config dictionary helpers (`get_mcp_cache_config()`, `get_mcp_store_config()`, `get_mcp_response_size_config()`) and routed MCP caching, Redis storage, and response-size guard factories through them. This centralizes the `MCP_CACHE_CONFIG`, `MCP_STORE_CONFIG`, and `MCP_RESPONSE_SIZE_CONFIG` keys while preserving each factory's app-context handling, defaults, and optional dependency behavior.
58. Added startup config helpers (`get_mcp_disabled_tools()`, `get_mcp_debug_enabled()`) and reused the existing `get_superset_app_name()` helper in FastMCP app initialization. This centralizes `MCP_DISABLED_TOOLS`, `MCP_DEBUG`, and startup branding reads while preserving lazy startup imports, disabled-tool removal behavior, and stdio debug-output behavior.
59. Added HTTP server startup config helpers (`get_superset_app_icon()`, `get_superset_webserver_address()`, `get_mcp_hello_page_config()`, `get_mcp_tool_search_config()`) and routed Starlette middleware/page setup plus tool-search configuration through them. This centralizes app icon, public webserver address, hello-page override, and tool-search config reads while preserving page-config composition, remote icon handling, relative icon URL construction, hello-page type validation, and tool-search defaults.
60. Added auth-provider selection config helpers (`get_mcp_auth_factory()`, `get_mcp_api_key_enabled_flag()`, `get_fab_api_key_enabled()`) and routed HTTP server auth-provider selection through them, alongside the existing `get_mcp_auth_enabled()` helper. This centralizes the startup decision reads for custom auth factories and auth enablement flags while preserving the existing factory precedence, fallback to the default auth factory, and raw truthiness behavior.
61. Added auth user-resolution config helpers (`get_mcp_jwt_issuer()`, `get_mcp_user_resolver()`, `get_mcp_api_key_create_url()`) and routed JWT user resolution plus API-key fallback error guidance through them. This centralizes multi-issuer warning, custom resolver, and API-key creation URL reads while preserving the existing default resolver behavior, warning condition, and `/profile/` fallback URL.
62. Routed `get_mcp_api_key_enabled()` through the shared config helper boundary for `MCP_API_KEY_ENABLED` and `FAB_API_KEY_ENABLED`. This centralizes the explicit-MCP-setting versus FAB-fallback read policy while preserving the existing `None` sentinel behavior, bool coercion, and startup warning semantics.
63. Added JWT auth factory config helpers (`get_mcp_jwt_audience()`, `get_mcp_jwks_uri()`, `get_mcp_jwt_public_key()`, `get_mcp_jwt_secret()`, `get_mcp_required_scopes()`, `get_mcp_jwt_debug_errors()`, `get_mcp_jwt_algorithm_with_default()`) and routed `mcp_config.py` JWT verifier construction through them. This centralizes raw JWT audience/key/scope/debug/algorithm reads while preserving audience fail-closed behavior, key-source fallback, HS256 handling, required-scope warning behavior, and the default RS256 algorithm.
64. Added `get_fab_api_key_prefixes()` and routed `mcp_config.py` composite API-key verifier setup through it. This removes the last direct FAB API-key prefix config read from `mcp_config.py` while preserving string/list prefix normalization, invalid-prefix fallback handling, and the default `["sst_"]` prefix.
65. Added `user_can_access_dataset_permission()` in `utils/permissions_utils.py` and routed data-model privacy checks through it. This removes direct `security_manager.can_access(..., "Dataset")` coupling from `privacy.py` while preserving the existing Dataset permission names, short-circuit behavior, and fail-closed exception handling.
66. Added `current_user_can_access()` in `utils/permissions_utils.py` and routed `get_schema` per-model RBAC through it. This removes direct `security_manager.can_access("can_read", class_permission)` coupling from schema discovery while preserving the existing RBAC flag check, permission/view names, denial logging, and structured permission error.
67. Routed `auth.check_tool_permission()` through `current_user_can_access()`. This removes direct `security_manager.can_access(permission, view)` coupling from the central MCP tool-permission path while preserving the permission string construction, RBAC-disabled shortcut, missing-user denial, token-scope intersection, denial logging, and boolean result behavior.
68. Added `current_user_can_access_database()` in `utils/permissions_utils.py` and routed SQL Lab database validation in `execute_sql` and `save_sql_query` through it. This removes direct `security_manager.can_access_database(database)` coupling from the SQL Lab MCP tools while preserving database lookup, denial messages, Superset error types, and execution/save behavior.

## Safe Cleanup Status

The low-risk/high-impact boundary cleanup pass is complete as of improvement #30.
Remaining findings are intentionally deferred because they require broader design
or medium-risk refactoring, not small reversible helper extraction.

Deferred boundary areas:

- **MCP tool modules and Superset globals** — still important, but extracting
  `event_logger`, `db`, `security_manager`, and Flask globals requires adapter
  design across tool execution and auth paths. Improvements #31–#45 add a
  narrow event-logging adapter for shared MCP utility code, the repeated
  list-tool query path, read-only get-info/layout lookup paths, and small
  system/discovery/read-explore/SQL Lab/dataset/chart-preview-update/
  chart-preview-retrieval/chart-data-retrieval/chart-generation/chart-update/
  dashboard utility paths, plus middleware event writes.
  Improvement #46 starts a narrow config adapter for chart data row-limit
  fallback. Improvement #47 extends the config adapter to shared APP_NAME
  branding reads in system tools/prompts. Improvement #48 adds the bug-report
  contact config read to the same narrow config boundary. Improvement #49 adds
  the shared MCP RBAC-enabled flag to that boundary. Improvement #50 moves
  auth user-resolution config reads into the same helper layer. Improvement #51
  adds the JWT algorithm config read used by verifier startup warnings.
  Improvement #52 adds the configured Superset admin-role name used by MCP
  field-permission checks. Improvement #53 starts consolidating screenshot
  timing/error config reads. Improvement #54 adds WebDriver type selection to
  the same screenshot/config boundary. Improvement #55 moves global WebDriver
  pool settings behind that boundary and removes direct Flask config access from
  the WebDriver pool module. Improvement #56 moves MCP URL construction config
  reads behind the same helper boundary and removes direct Flask config access
  from URL utilities. Improvement #57 moves MCP cache/store/response-size
  factory config dictionaries behind that helper boundary. Improvement #58
  moves FastMCP startup branding, disabled-tool, and stdio debug reads behind
  the same helper boundary. Improvement #59 moves HTTP server startup page and
  tool-search config reads behind the same helper boundary. Improvement #60
  moves HTTP server auth-provider selection config reads behind that helper
  boundary without changing auth construction logic. Improvement #61 moves
  auth user-resolution and API-key fallback URL reads behind the same helper
  boundary. Improvement #62 moves MCP/FAB API-key enablement fallback reads in
  the auth config factory behind that helper boundary. Improvement #63 moves
  JWT auth factory reads in `mcp_config.py` behind the same helper boundary.
  Improvement #64 moves FAB API-key prefix reads behind that boundary as well.
  Improvement #65 starts the next narrow security-manager boundary by routing
  Dataset-level metadata permission checks through `permissions_utils.py`.
  Improvement #66 routes schema-discovery RBAC checks through the same
  permission boundary.
  Improvement #67 routes central MCP tool RBAC checks through that boundary.
  Improvement #68 adds the database-access check to the same permission
  boundary for SQL Lab tools.
- **Frontend list pages** — deeper cleanup would involve shared list-page
  orchestration/hooks beyond the safe page-size constant extraction.
- **Report command workflow boundaries** — timestamp consistency is fixed; the
  remaining workflow/state separation would be a broader command refactor.
- **Pandas postprocessing compatibility** — existing compatibility branches are
  small and tested; extracting them now would add more risk than value.

Next work should start only after choosing one deferred area explicitly and
planning a medium-risk refactor with wider tests.
